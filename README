A plugin to change the status bar to be better looking.

Column number not showing.  No idea why.  Needs fixed.

Colors are directly in the plugin.  To make it customizable, I need to have them as options.
Need a flag to determine whether the file has already been sourced.
